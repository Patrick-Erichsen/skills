interface:
  display_name: "Linear Agent Project Planning"
  short_description: "Plan Linear queues for agent runs."
  default_prompt: "Use $linear-agent-project-planning to open a reviewable Linear root issue first, then split it into an executable task queue after approval."
