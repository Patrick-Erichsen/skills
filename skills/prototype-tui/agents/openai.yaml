interface:
  display_name: "Prototype TUI"
  short_description: "Compare isolated TUI variants in tmux"
  default_prompt: "Use $prototype-tui to isolate a terminal UI with fixture data and compare multiple interactive variants in tmux."
